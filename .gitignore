# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Secret keys must be unknown
secrets.yaml

.vscode/ 
.idea/
bin/
/pkg/server/grpc/client/
